{% extends 'base.html.twig' %}

{% block title %}Firm index{% endblock %}

{% block body %}
    <h1>{{ 'firm.list'|trans }}</h1>
    <a href="{{ path('firm_new') }}" class="btn btn-primary float-right">{{ 'action.create'|trans }}</a>
    {% include 'components/organismes/firm/list.html.twig' with {'firms': firms} only %}
    <div class="navigation float-right">
        {{ knp_pagination_render(firms) }}
    </div>
{% endblock %}
